<!--<?xml version="1.0" encoding="utf-8" ?>-->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp3.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
        </VerticalStackLayout>
        <VerticalStackLayout Padding="20" Spacing="15">
            <DatePicker Date="{Binding Date}"/>
            <Label Text="{Binding DateText}" VerticalOptions="Center"/>
            <HorizontalStackLayout Spacing="10">
                <Picker ItemsSource="{Binding AvableCur}" SelectedItem="{Binding FromCurrency}" WidthRequest="100"/>
                <Entry Text="{Binding value}" FontSize="14" TextColor="White" VerticalOptions="Center" MinimumWidthRequest="200"/>
            </HorizontalStackLayout>
            <HorizontalStackLayout Spacing="10">
                <Picker ItemsSource="{Binding AvableCur}" SelectedItem="{Binding ToCurrency}" WidthRequest="100"/>
                <Entry Text="{Binding value2}" FontSize="14" TextColor="White" VerticalOptions="Center" MinimumWidthRequest="200"/>
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
